# Ignore everything in this directory because we don't want to store binaries in git
*
# Except this file and the npmignore file (which will allow us to include binaries in the npm package)
!.gitignore
!.npmignore
